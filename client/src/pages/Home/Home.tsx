import DrawerLayout from '../../components/Drawer/DrawerLayout';

const Home = (): JSX.Element => {
    return (
        <DrawerLayout>
            <div>Home page</div>
        </DrawerLayout>
    );
};

export default Home;
